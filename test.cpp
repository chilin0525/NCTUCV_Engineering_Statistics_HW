#include <bits/stdc++.h>
using namespace std;

int main() {
    cin.tie(0), ios::sync_with_stdio(0);
    
    int x=1;
    if(true)
        if(false)
            x=2;
        else
            x=3;

    cout<<x<<endl;
    return 0;
}